!#/bin/bash
set -x # Print commands and their arguments as they are executed. debug mode
set -e # Exit immediately if a command exits with a non-zero status. error handling
set -o pipefail # Return the exit status of the last command in the pipeline that failed. error handling


